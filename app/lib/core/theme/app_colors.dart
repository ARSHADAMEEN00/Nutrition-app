import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette (Sage Green & Soft Salmon)
  static const Color primary = Color(0xFF8DA399); // Sage Green
  static const Color primaryDark = Color(0xFF6B8278);

  static const Color secondary = Color(0xFFECA89F); // Soft Salmon/Peach
  static const Color secondaryLight = Color(0xFFF9DCD8);

  // Backgrounds
  static const Color background = Color(0xFFFAFAFA); // Off-white/Cream
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF2D3142); // Dark Charcoal
  static const Color textSecondary = Color(0xFF909399); // Soft Grey

  // Inputs
  static const Color inputBackground = Color(0xFFF5F6F5);
  static const Color inputBorder = Color(0xFFE0E0E0);

  // Status
  static const Color error = Color(0xFFE57373);
  static const Color success = Color(0xFF81C784);
}
